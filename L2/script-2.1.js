alert('Please enter the nubmer one by one to calculate the average?');

const x = Number(prompt('first number = '));
const y = Number(prompt('second number = '));

alert(`Arighmetic average of ${x} and ${y} => ${(x + y) / 2}`);